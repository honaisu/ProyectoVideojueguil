package interfaces;

/**
 * Interfaz que provee una ruta válida a conseguir.
 * <p>
 * Sirve específicamente para los enumeradores que proveen rutas a algún asset específico.
 */
public interface AssetRoute {
	public String getRuta();
}
